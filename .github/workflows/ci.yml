name: CI Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  scan-virus:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Scan for viruses
        uses: hugoalh/scan-virus-ghaction@latest
        if: 1 == 2  # This skips the virus scan for now, but can be removed later.
