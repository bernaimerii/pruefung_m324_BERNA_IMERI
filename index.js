import chalk from 'chalk';

console.log(chalk.blue('It works'))

let myUnusedVariable = 10; // Diese Variable wird deklariert, aber nie verwendet
let variable = 20;

if (variable == 10) {  // Hier wird '==' verwendet
  console.log('Test');
}